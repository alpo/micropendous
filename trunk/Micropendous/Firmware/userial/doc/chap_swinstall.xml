<chapter id="sw_installation">
    <title>Installation</title>
    <para>
    The &userial; board is a USB communications device class (CDC) device and is visible as a virtual COM port to the user.
    Most Operating Systems already include drivers for this kind of device.
    </para>
    <para>
    The serial settings are not important for the USB communication. The values of 115200 bps, 8 data bits, no parity, 1 stop bit are a safe choice.
    Hardware handshake should be turned off.
    </para>
    <sect1 id="sw_installation_linux">
        <title>Installation on Linux, BSD, OS X</title>
        <para>
        Virtually all UNIX systems include an ACM driver already in their kernel.
        Under Linux this device is accessible as a <filename class='devicefile'>/dev/ttyACMx</filename> device file.
        The device can be opened using a serial terminal such as <application>miniterm</application> or <application>cutecom</application>.
        </para>
    </sect1>
    <sect1 id="sw_installation_windows">
        <title>Driver installation on Microsoft Windows</title>
        <para>
        Users of Microsoft Windows need to install the file <filename>userial.inf</filename> when the Hardware Wizard searches for new devices.
        There are many serial terminals for Windows out there to access the userial device. Some of them are:
        <application>Hyperterm</application>, <application>TeraTerm</application> or <application>Br@y terminal</application>.
        </para>
    </sect1>
</chapter>
